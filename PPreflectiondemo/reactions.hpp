#pragma once
#include "animals.hpp"

namespace reactions
{
	void react_to(const animals::ape& ape);
	void react_to(const animals::bat& bat);
	void react_to(const animals::cat& cat);
	void react_to(const animals::dog& dog);
	void react_to(const animals::elk& elk);
}
